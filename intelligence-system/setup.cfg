[tool:pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    -v
    --tb=short
    --strict-markers
    -m "not integration"

[coverage:run]
source = .
omit =
    tests/*
    .env*
    setup.cfg

[coverage:report]
show_missing = True
skip_covered = False
